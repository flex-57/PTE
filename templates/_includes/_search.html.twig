<form action="{{ path('app_search') }}" method="get" class="flex items-center w-full max-w-md">
        {{ form_widget(searchForm.q, {
            'attr': {
                'class': 'w-full px-4 py-2 border border-gray-300 rounded-l-md focus:outline-none focus:ring-2 focus:ring-blue-500',
            }
        }) }}
        <button type="submit" class="px-4 py-2 border border-slate-400 bg-slate-400 text-white font-semibold rounded-r-md hover:bg-blue-600 transition-colors">
            🔍
        </button>
</form>