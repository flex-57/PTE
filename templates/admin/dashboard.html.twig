{% extends '@EasyAdmin/page/content.html.twig' %}

{% block content %}
    <div class="container">
        <h1 class="text-center mb-5">Administration EPT</h1>

        <div class="row justify-content-center">
            {% for card in cards %}
                {% include 'admin/_card.html.twig' with card %}
            {% endfor %}
        </div>
    </div>
{% endblock %}
