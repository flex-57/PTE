<div class="col-xl-4 col-md-5 mb-4 d-flex">
    <div class="card w-100 d-flex flex-column"
         style="background: linear-gradient(135deg, {{ gradient_start }} 0%, {{ gradient_end }} 100%);">
        <div class="card-body d-flex flex-column flex-grow-1">
            <h5 class="card-title">
                <i class="{{ icon }} me-2"></i>
                {{ title }}
                <span class="badge" style="background-color: {{ badge_color }}; color: #fff;">
                    {{ count }}
                </span>
            </h5>

            <p class="card-text flex-grow-1">{{ description }}</p>

            <div class="mt-auto">
                <a href="{{ ea_url().setController(controller).setAction('index').generateUrl() }}"
                   class="btn btn-primary w-auto">
                    Gérer
                </a>
            </div>
        </div>
    </div>
</div>
